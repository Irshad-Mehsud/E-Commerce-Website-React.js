import { createContext } from "react";
const AddToCartContext = createContext(); // Create a context for managing cart state
export default AddToCartContext; // Export the context for use in other components